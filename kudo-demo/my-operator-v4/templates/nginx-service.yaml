apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    run: my-nginx
  name: my-nginx
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
    nodePort: {{ .Params.nodePort }}
  selector:
    run: my-nginx
  type: NodePort
